 <!--main navigation-->
 <nav class="navbar navbar-expand-lg bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand"><img src="/assets/AK_logo.png" width="30" height="30" alt=""></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
       
        <ul class="navbar-nav  ms-auto">
          <% if (title =="Home"){%>
            <li class="nav-item active">
            <%} else {%><li class="nav-item">
            <% } %>
            <a class="nav-link active text-warning" href="/home"><i class="fa-regular fa-font-awesome"></i> Home</a>
            </li>
            <% if (title =="About"){%>
              <li class="nav-item active">
              <%} else {%><li class="nav-item">
              <% } %>
               <a class="nav-link active text-warning" href="/about"><i class="fa-regular fa-address-card"></i> About</a>
              </li>
              <% if (title =="Contact"){%>
                <li class="nav-item active">
                <%} else {%><li class="nav-item">
                <% } %>
            <a class="nav-link text-warning " href="/contact"><i class="fa-regular fa-comment"></i> Contact</a>
          </li>
          <% if (title =="Services"){%>
            <li class="nav-item active">
            <%} else {%><li class="nav-item">
            <% } %>
            <a class="nav-link text-warning " href="/services"><i class="fa-regular fa-handshake"></i> Services</a>
          </li>
          <% if (title =="Projects"){%>
            <li class="nav-item active">
            <%} else {%><li class="nav-item">
            <% } %>
            <a class="nav-link text-warning" href="/projects"><i class="fa-solid fa-laptop-code"></i> Projects</a>
          </li>
          <% if (title =="Books"){%>
            <li class="nav-item active">
            <%} else {%><li class="nav-item">
            <% } %>
            <a class="nav-link text-warning" href="/book-list"><i class="fa-solid fa-book"></i> Books</a>
          </li>
        </ul>
        <% if(typeof displayName == 'string' && displayName) { %>
          <li class="navbar-text text-warning"><i class="fas fa-lg fa-user text-warning"></i> Welcome, <%= displayName %></li>
          <li class="nav-item"><a href="/logout" class="nav-link text-warning">
            <i class="fas fa-lg fa-sign-out-alt"></i> Logout</a></li>
        <% } else { %>
          <li class="nav-item"><a href="/login" class="nav-link text-warning">
            <i class="fas fa-lg fa-sign-in-alt"></i> Login</a></li>
        <% } %>
    </div>
  </nav>